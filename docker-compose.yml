services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: aboutmesite-web
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${FLASK_PORT:-5000}:5000"
    volumes:
      - ./database:/app/database
